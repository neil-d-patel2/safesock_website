#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typescript/bin/tsserver" "$@"
else
  exec node  "$basedir/../../../../typescript/bin/tsserver" "$@"
fi
# cmd-shim-target=/Users/neil/Dev/safesock/node_modules/.pnpm/@typescript-eslint+utils@8.56.1_eslint@9.39.4_jiti@2.6.1__typescript@5.9.3/node_modules/typescript/bin/tsserver
